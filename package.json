{
  "name": "frontpgfit",
  "version": "1.0.0",
  "private": true,
  "description": "PGFit landing standalone — sistema operativo financiero para gimnasios, pilates y academias",
  "scripts": {
    "build": "echo 'static site, no build needed' && exit 0"
  }
}
